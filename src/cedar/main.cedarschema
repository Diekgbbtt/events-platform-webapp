entity Category;
entity Log;
entity Consent;
entity Purpose;

entity EventsContainer;
entity CategoriesContainer;
entity AdsContainer;
entity Role;

entity Person in Role;

entity Event;

action readEventRequesters, addEvent, analyze appliesTo {
    principal: Person,
    resource: Event
}
